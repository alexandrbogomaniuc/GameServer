import DialogInfo from '../DialogInfo';

class LeaveRoomDialogInfo extends DialogInfo
{
	constructor(dialogId, priority)
	{
		super(dialogId, priority);
	}

	destroy()
	{
		super.destroy();
	}
}

export default LeaveRoomDialogInfo