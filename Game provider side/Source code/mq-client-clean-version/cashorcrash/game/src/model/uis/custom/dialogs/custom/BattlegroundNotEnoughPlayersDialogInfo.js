import DialogInfo from '../DialogInfo';

class BattlegroundNotEnoughPlayersDialogInfo extends DialogInfo
{
	constructor(dialogId, priority)
	{
		super(dialogId, priority);
	}

	destroy()
	{
		super.destroy();
	}
}

export default BattlegroundNotEnoughPlayersDialogInfo