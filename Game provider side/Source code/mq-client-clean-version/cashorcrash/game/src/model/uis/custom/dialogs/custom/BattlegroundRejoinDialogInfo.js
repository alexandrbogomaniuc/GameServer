import DialogInfo from '../DialogInfo';

class BattlegroundRejoinDialogInfo extends DialogInfo
{
	constructor(dialogId, priority)
	{
		super(dialogId, priority);
	}

	destroy()
	{
		super.destroy();
	}
}

export default BattlegroundRejoinDialogInfo