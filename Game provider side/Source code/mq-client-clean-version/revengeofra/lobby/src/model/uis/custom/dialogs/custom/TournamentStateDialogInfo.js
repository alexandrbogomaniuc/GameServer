import DialogInfo from '../DialogInfo';

class TournamentStateDialogInfo extends DialogInfo
{
	constructor(dialogId, priority)
	{
		super(dialogId, priority);
	}
}

export default TournamentStateDialogInfo