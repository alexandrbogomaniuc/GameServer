import CalloutInfo from '../CalloutInfo';

class DarkKnightCalloutInfo extends CalloutInfo
{
	constructor(calloutId, priority)
	{
		super(calloutId, priority);
	}
}

export default DarkKnightCalloutInfo